To execute these files in the terminal type:
$ gcc fileName.c -lGLU -lGL -lglut -lm 


The compiler shall be g++ for .cpp files (Means replace gcc with g++ filename.cpp in above code) and 
the -lm is only for programs that use the Math function.
